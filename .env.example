# ─── Kalshi API credentials (required for all authenticated endpoints) ────────
KALSHI_API_KEY_ID=your-api-key-id-here
KALSHI_PRIVATE_KEY_PATH=kalshi.pem
# "demo" or "prod" — default: demo
# Note: API keys are environment-specific; a prod key returns 401 on demo and vice versa
KALSHI_ENV=demo

# ─── kalshi_sports_edge — LLM provider (one required when using --llm) ────────
# Anthropic Claude (default provider: --provider claude)
ANTHROPIC_API_KEY=your-anthropic-api-key-here
# Moonshot Kimi (alternative provider: --provider kimi)
KIMI_API_KEY=your-kimi-api-key-here

# ─── kalshi_sports_edge — Web search (optional, required for --web-search) ────
BRAVE_SEARCH_API_KEY=your-brave-search-api-key-here

# ─── Demo script helpers (optional, used by specific scripts in 05_orders/ etc.) ─
# Pre-set a market ticker so demo scripts skip the auto-lookup step
KALSHI_EXAMPLE_TICKER=
# Pre-set an order ID for 05_orders/02_get_order.py and 06_cancel_order.py
KALSHI_EXAMPLE_ORDER_ID=
# Pre-set an order group ID for 07_order_groups/03_order_with_group.py
KALSHI_EXAMPLE_GROUP_ID=

# IMPORTANT: Never put inline comments after = values.
# python-dotenv includes everything after = (including # comments) as the value.
# Always put comments on their own lines, as shown above.
